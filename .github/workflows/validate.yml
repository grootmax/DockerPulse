name: Validation and Testing Pipeline

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]

jobs:
  validate-and-test:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install System Dependencies
        run: sudo apt-get update && sudo apt-get install -y libglib2.0-bin

      - name: Install Dependencies
        run: npm ci

      - name: Stage Extension Artifacts
        run: make stage

      - name: Audit Build Manifest
        run: node audit-manifest.js

      - name: Run Syntax and Linter Validation (ESLint)
        run: npx eslint .

      - name: Run JSDoc Type-Checking (TSC)
        run: npx tsc --project jsconfig.json

      - name: Run Unit Tests
        run: npm test
